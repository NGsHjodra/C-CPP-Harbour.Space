#include <iostream>
#include <cassert>

using namespace std;

void strcpy(char *to, const char *from) {
    // TODO: Given string `from`, copy its content to buffer `to`.
    //       You must assume that the buffer has already been allocated.
}

int main() {
    // TODO: tests
    cout << "OK" << endl;
}
